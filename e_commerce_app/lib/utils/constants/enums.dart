// List of enums
// Enums are classes they cant be created inside class

enum TextSizes { small, medium, large }

enum OrderStatus {
  processing,
  shipped,
  delivered,
}

enum PaymentMethod {
  payPal,
  googlePay,
  applePay,
  visa,
  masterCard,
  creditCard,
  payStack,
  razorPay,
  paytm
}
